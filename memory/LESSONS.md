# Lessons Learned — trailguard

> Durable, codebase-specific lessons. Different from workspace `~/.openclaw/workspace/memory/lessons.md` — this is project-scoped.
>
> When to add: a non-obvious fact you'd want to know on a fresh session in this codebase. Build flags, library quirks, debugging traps, things that broke once and shouldn't again.
>
> Format: short bullets, one fact each. Group by topic when it grows.

## Build / Tooling
_(e.g. "xcodegen needs `-skipMacroValidation` flag for CLI builds")_

## Patterns / Architecture
_(e.g. "DataManager.holdings filters archived; use allHoldings to write")_

## Gotchas
_(e.g. "Simulator unreliable for GoogleSignIn — test on real device")_
